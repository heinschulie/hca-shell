import { RouterConfig }       from '@angular/router';
import { PreRegisterComponent }     from './pre-register.component';
export const preregisterRoutes: RouterConfig = [
  { path: 'preregister', component: PreRegisterComponent }
];
